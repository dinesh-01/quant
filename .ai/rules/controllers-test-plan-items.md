---
paths:
  - 'app/Http/Controllers/TestPlanItems/**'
---

# Controllers Test Plan Items

## Plan item writes bind the plan or the item
Link and reorder are nested under the plan. Urgency, version bump and unlink bind the item. Each is its own form on the contents page — do not nest them. Reorder posts the full order[] like the specification tree.
